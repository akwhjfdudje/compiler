    .globl main
main:
    push     %ebp
    movl     %esp, %ebp
    # statement
    movl     $0, %eax
    pushl    %eax
    # statement
    # Unknown AST Node
    # statement
    # statement
    movl     $0, %eax
    movl     %eax, %eax
    movl     %ebp, %esp
    pop      %ebp
    ret
